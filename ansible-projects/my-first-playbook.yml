---
- name: Install and start nginx
  hosts: all
  become: true
  tasks:
   - name: Intall nginx
     apt:
      name: nginx
      state: present
   - name: Start nginx
     service:
      name: nginx
      state: started
